{
  "name": "sync-watch-room",
  "version": "1.0.0",
  "description": "Mobile-first synchronized movie-watching room with neon UI, host-controlled playback sync, and fullscreen floating chat.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
